DJANGO_SECRET_KEY=change-me-to-a-real-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1,https://lingolist.io,https://www.lingolist.io
SQLITE_PATH=db.sqlite3
LIBRETRANSLATE_URL=http://localhost:5000
# Optional for local development. If unset, app uses SQLITE_PATH.
# Required for production PostgreSQL, for example:
# DATABASE_URL=postgresql://lingolist_user:strong-password@postgres:5432/lingolist
DATABASE_URL=
